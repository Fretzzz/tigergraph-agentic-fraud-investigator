import{writeFile,mkdir}from"node:fs/promises";import{validateCapabilities,type CapabilityObservations}from"../packages/capabilities/src/index.js";const live=process.argv.includes("--live");const target=process.env.APPROVED_LIVE_TARGET;if(!live||!target){console.error("BLOCKED: --live and APPROVED_LIVE_TARGET are required. Missing provider access cannot pass this gate.");process.exit(2)}const required=["NVIDIA_API_KEY","NIM_BASE_URL","NIM_MODEL","MCP_SERVICE_TOKEN","TG_MCP_URL","TIGERGRAPH_HOST","TIGERGRAPH_GRAPH"];const missing=required.filter(k=>!process.env[k]);if(missing.length){console.error(`BLOCKED: missing configuration names: ${missing.join(", ")}`);process.exit(2)}const observations:CapabilityObservations={nimToolRoundTrip:false,nimUsage:false,mcpInitialize:false,mcpAllowedQuery:false,tigergraphVectorResult:false,doSqlRestart:false};const report={target,generatedAt:new Date().toISOString(),...validateCapabilities(observations),blocker:"Probe transports are not implemented until approved provider identities and isolated resources are confirmed."};await mkdir("reports/capabilities",{recursive:true});await writeFile("reports/capabilities/latest.json",`${JSON.stringify(report,null,2)}\n`);console.error("BLOCKED: provider configuration exists, but no live capability has been observed yet.");process.exit(2);
