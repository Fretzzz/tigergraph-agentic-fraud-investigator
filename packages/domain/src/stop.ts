import type{Assessment}from"../../contracts/src/domain.js";import{independentEvidenceCount,type EvidenceForIndependence}from"./evidence-independence.js";export interface StopDecision{settled:boolean;reason:string;operationalState:"complete"|"running"|"blocked"}export function decideStop(a:Assessment,evidence:readonly EvidenceForIndependence[],unresolved:readonly string[],budget:{exhausted:boolean;timedOut:boolean}):StopDecision{if(budget.exhausted||budget.timedOut)return{settled:false,reason:budget.timedOut?"Provider timeout is not evidence":"Budget exhaustion is not evidence",operationalState:"blocked"};const threshold=a.fraudProbability>=.85||a.fraudProbability<=.15;if(threshold&&independentEvidenceCount(evidence)>=2)return{settled:true,reason:"Probability threshold has two independent evidence families",operationalState:"complete"};if(unresolved.length===0&&a.suggestedStopReason)return{settled:true,reason:a.suggestedStopReason,operationalState:"complete"};return{settled:false,reason:"Evidence remains insufficient or unresolved",operationalState:"running"};}
