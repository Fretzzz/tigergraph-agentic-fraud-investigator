import{AnswerSchema,type Answer}from"../../contracts/src/answer.js";import type{SourceIndex}from"./validate.js";export interface CompileMetrics{toolCalls:number;tokens:number;latencySeconds:number}export function compileAnswer(snapshot:Answer,_sources:SourceIndex,metrics:CompileMetrics):Answer{return AnswerSchema.parse({...structuredClone(snapshot),tool_calls:metrics.toolCalls,tokens:metrics.tokens,latency_s:metrics.latencySeconds});}
